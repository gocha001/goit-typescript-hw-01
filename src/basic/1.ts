let age: number;
age = 50;
let username: string;
username = 'Max';
let toggle: boolean;
toggle = true;
let empty: null;
empty = null;

const callback = (a: number): number => { return 100 + a };
console.log(callback);
